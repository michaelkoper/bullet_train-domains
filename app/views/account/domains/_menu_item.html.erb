<% if can? :read, Domain.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :domains]),
    label: t('domains.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('domains.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
